<%- include("../partials/header", {headerContent:"Password change"}) %>

<main class="password-page">
    <section class="password-card">
        <div class="password-header">
            <h4>Change password</h4>
            <p>To change your password, please fill the form below
            </p>
        </div>
            <form action="/users/<%=user.id %>/change_password?_method=PATCH" method="post" class="password-form">
                <div class="form-group">
                    <label for="current_password">
                        Current password:
                        <span>*</span>
                    </label>
                    <input
                        type="password"
                        id="cur_pwd"
                        name="currentPassword"
                        required
                    >
                </div>
                <div class="form-group">
                    <label for="new_password">
                        New password:
                        <span>*</span>
                    </label>
                    <input
                        type="password"
                        id="new_pwd"
                        name="newPassword"
                        required
                    >
                </div>
                <div class="form-group">
                    <label for="con_new_password">
                        Confirm new password:
                        <span>*</span>
                    </label>
                    <input
                        type="password"
                        id="con_new_pwd"
                        name="confirmedNewPassword"
                        required
                    >
                </div>
                <small class="message"><%=locals.message%></small>
                <div class="form-actions">
                    <button type="submit" class="btn primary">
                        Submit
                    </button>
                </div>
                <small>
                    <a href="/profile" class="go-back">Back to profile details</a>
                </small>
            </form>
        </div>
    </section>
</main>

<%- include("../partials/footer") %>